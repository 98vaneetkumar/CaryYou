<div class="main-content">
  <section class="section">
    <div class="section-header">
      <h1><%= viewuser.name %></h1>
      <div class="section-header-breadcrumb">
        <div class="breadcrumb-item active">
          <a href="/admin/dashboard">Dashboard</a>
        </div>
        <div class="breadcrumb-item">Restaurant</div>
      </div>
    </div>

    <!-- Grid Container -->
    <div class="restaurant-grid">
      <% viewuser.products.forEach(function(data, i) { %>
        <div class="restaurant-item">
          <div class="restaurant-image-container">
            <div class="restaurant-images">
              <% data.images.forEach(function(image, index) { %>
                <div class="restaurant-image">
                  <img
                    src="<%= image ? image : 'https://avatar.iran.liara.run/public/boy?username=Ash' %>"
                    alt="<%= data.itemName %>"
                    class="img-fluid"
                  />
                </div>
              <% }); %>
            </div>

            <% if (data.images.length > 1) { %>
              <button class="carousel-arrow left-arrow" onclick="moveSlide('left', '<%= i %>')">&#10094;</button>
              <button class="carousel-arrow right-arrow" onclick="moveSlide('right', '<%= i %>')">&#10095;</button>
            <% } %>
          </div>

          <div class="restaurant-info">
            <h5><%= data.itemName %></h5>
            <p class="sub-category"><%= data?.subCategoryName %></p>
            <p class="price">$<%= data.price %></p>
            <div class="buttons-container">
              <div class="status-btn">
                <% if (data?.status == 1) { %>
                  <button
                    id="restaurantId_<%= data._id %>"
                    class="btn btn-sm btn-success status"
                    data-id="<%= data._id %>"
                    value="0"
                  >
                    Active
                  </button>
                <% } else { %>
                  <button
                    id="restaurantId_<%= data._id %>"
                    class="btn btn-sm btn-danger status"
                    data-id="<%= data._id %>"
                    value="1"
                  >
                    In-Active
                  </button>
                <% } %>
              </div>
              <div class="view-btn">
                <button 
                  class="btn btn-sm bg-warning view-product-btn" 
                  data-id="<%= data._id %>" 
                  data-toggle="modal" 
                  data-target="#productDetailModal">
                  <i class="fa fa-eye" aria-hidden="true"></i>
                </button>
              </div>
            </div>
          </div>
        </div>
      <% }); %>
    </div>
  </section>
</div>

<!-- "Top Up" Button -->
<button id="topUpButton" class="topup-btn">
  ↑
</button>

<style>
  /* Grid Styles */
  .restaurant-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 20px;
    padding: 15px;
  }

  .restaurant-item {
    background: #fff;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
    display: flex;
    flex-direction: column;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .restaurant-item:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 20px rgba(0, 0, 0, 0.15);
  }

 /* "Top Up" Button Styles */
.topup-btn {
  position: fixed;
  bottom: 20px;
  right: 20px;
  background-color: #00000094; /* Green color for a more inviting look */
  color: white;
  border: none;
  border-radius: 100%;
  padding: 15px;
  font-size: 24px; /* Larger font size */
  cursor: pointer;
  z-index: 1000;
  display: none;
  box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2); /* Softer shadow */
  transition: background-color 0.3s ease, transform 0.3s ease;
  transform: scale(1); /* Start with normal size */
}

.topup-btn:hover {
  background-color: #000000; /* Darker green on hover */
  transform: scale(1.1); /* Slightly enlarge the button on hover */
  box-shadow: 0 12px 25px rgba(0, 0, 0, 0.3); /* Darker shadow on hover */
}

.topup-btn:active {
  box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2); /* Restore shadow on click */
}


  /* Restaurant Grid Styles */
  .restaurant-image-container {
    position: relative;
    width: 100%;
    overflow: hidden;
    border-radius: 12px 12px 0 0;
  }

  .restaurant-images {
    display: flex;
    transition: transform 0.5s ease;
  }

  .restaurant-image {
    min-width: 100%;
    transition: opacity 0.5s ease;
  }

  .restaurant-image img {
    width: 100%;
    height: 200px;
    object-fit: cover;
    border-radius: 12px 12px 0 0;
  }

  .carousel-arrow:focus {
    outline: none;
    border: none;
  }

  .carousel-arrow {
    position: absolute;
    top: 50%;
    z-index: 10;
    background-color: transparent;
    color: white;
    border: none;
    padding: 15px;
    font-size: 20px;
    cursor: pointer;
    transform: translateY(-50%);
    border-radius: 50%;
    outline: none;
  }

  .left-arrow {
    left: 10px;
  }

  .right-arrow {
    right: 10px;
  }

  .restaurant-info {
    padding: 12px 10px;
    text-align: center;
  }

  .restaurant-info h5 {
    margin: 8px 0;
    font-size: 18px;
    font-weight: bold;
    color: #333;
  }

  .restaurant-info .sub-category {
    font-size: 14px;
    color: #777;
    margin-bottom: 8px;
  }

  .price {
    font-size: 16px;
    font-weight: bold;
    color: #1e9e35;
    margin: 8px 0;
  }

  .buttons-container {
    display: flex;
    justify-content: space-between;
    margin-top: 12px;
  }

  .status-btn {
    margin-right: 5px;
  }

  .view-btn {
    margin-left: 5px;
  }

  .btn {
    font-size: 12px;
    padding: 5px 10px;
    border-radius: 20px;
  }

  /* Modal Styles */
  .modal-lg {
    max-width: 80% !important;
  }

  .modal-dialog {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .modal-content {
    width: 100%;
    height: auto;
    border-radius: 10px;
  }

  .modal-header {
    border-bottom: 1px solid #ddd;
  }

  .modal-footer {
    border-top: 1px solid #ddd;
  }
</style>

<script>
  // Handle Top Up Button visibility and smooth scroll to top
  window.onscroll = function() {
    // Show the button after scrolling 100px down
    if (document.body.scrollTop > 100 || document.documentElement.scrollTop > 100) {
      document.getElementById("topUpButton").style.display = "block";
    } else {
      document.getElementById("topUpButton").style.display = "none";
    }
  };

  // When the button is clicked, smoothly scroll to the top
  document.getElementById("topUpButton").onclick = function() {
    window.scrollTo({
      top: 0,
      behavior: "smooth"
    });
  };
</script>

<script>
  $(document).on('click', '.view-product-btn', function () {
    const productId = $(this).data('id');

    $.ajax({
      url: `/admin/restaurant_product_view/${productId}`,
      method: 'GET',
      success: function (response) {
        $('#productDetailContent').html(`
          <div class="text-center">
            <h4>${response.itemName}</h4>
            <p>${response.description || 'No description available.'}</p>
            <img src="${response.image}" alt="${response.itemName}" class="img-fluid">
            <p class="price">$${response.price}</p>
          </div>
        `);
      },
      error: function () {
        $('#productDetailContent').html('<p class="text-danger">Failed to load product details.</p>');
      },
    });
  });
</script>

<script>
  let currentSlideIndex = {};

  function moveSlide(direction, index) {
    const imagesContainer = document.querySelectorAll('.restaurant-images')[index];
    const totalImages = imagesContainer.children.length;

    if (!currentSlideIndex[index]) {
      currentSlideIndex[index] = 0;
    }

    if (direction === 'left') {
      if (currentSlideIndex[index] > 0) {
        currentSlideIndex[index] -= 1;
      }
    } else if (direction === 'right') {
      if (currentSlideIndex[index] < totalImages - 1) {
        currentSlideIndex[index] += 1;
      }
    }

    imagesContainer.style.transform = `translateX(-${currentSlideIndex[index] * 100}%)`;
  }
</script>

<script>
  $(document).ready(function () {
    $("#myTable").DataTable({
      dom: "Bfrtip",
      buttons: ["excel", "pdf", "print"],
    });
  });
</script>

<script>
  setTimeout(function () {
    $("div.msg").fadeOut();
  }, 2000);
</script>

<script>
  function confirmDelete(id) {
    const swalWithBootstrapButtons = Swal.mixin({
      customClass: {
        confirmButton: "btn btn-warning",
        cancelButton: "btn btn-info",
      },
      buttonsStyling: true,
    });

    swalWithBootstrapButtons
      .fire({
        title: "Are you sure?",
        text: "You won't be able to revert this!",
        icon: "warning",
        showCancelButton: true,
        confirmButtonText: "Yes, delete it!",
        cancelButtonText: "No, keep it",
        reverseButtons: true,
      })
      .then((result) => {
        if (result.isConfirmed) {
          $.ajax({
            url: `/admin/restaurant_product_delete/${id}`,
            method: "POST",
            success: function () {
              location.reload();
            },
            error: function () {
              swalWithBootstrapButtons.fire(
                "Error!",
                "There was an issue deleting the product.",
                "error"
              );
            },
          });
        }
      });
  }
</script>

<script>
  const changeStatus = (val) => {
    let id = val.getAttribute("data-id");
    let value = val.getAttribute("value");
    let elemID = val.getAttribute("id");
    elemID = document.getElementById(elemID);

    $.ajax({
      type: "POST",
      url: "/admin/restaurant_status",
      data: {
        id: id,
        value: value,
      },
      success: function (data) {
        if (data) {
          elemID.setAttribute("value", 0);
          elemID.innerText = `Active`;
          elemID.classList.remove("btn-danger");
          elemID.classList.add("btn-success");
        } else {
          elemID.setAttribute("value", 1);
          elemID.innerText = `In-Active`;
          elemID.classList.remove("btn-success");
          elemID.classList.add("btn-danger");
        }
        window.location.reload();
      },
    });
  };
</script>